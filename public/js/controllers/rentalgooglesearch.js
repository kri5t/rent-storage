'use strict';

angular.module('mean.articles').controller('RentalSearchController', ['$scope', '$stateParams', '$location', 'Global', function ($scope, $stateParams, $location, Global) {
    $scope.global = Global;

  // $scope.gPlace;
}]);