'use strict';

angular.module('mean.articles').controller('RentalSearchController', ['$scope', '$stateParams', '$location', 'Global', 'google-maps', function ($scope, $stateParams, $location, Global) {
    $scope.global = Global;

  // $scope.gPlace;
}]);